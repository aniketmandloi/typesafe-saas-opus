# ADR-0010: Node refuses type stripping for any file whose realpath sits under
# node_modules. `isolated` keeps each workspace package's realpath outside it,
# which is what lets bare Node run the raw source. Asserted at preinstall.
node-linker=isolated
